```javascript
function debounce(func, timeout){
    let timer = null
    return function () {
        clearTimeout(timer)
        timer = setTimeout(func, timeout)
    }
}
//初始版本，匿名函数scope内包括了它自身的AO与debounce函数context.AO，可以读取到其中的timer变量

```